# Domínio local (mDNS/Avahi)
HOMESERVER_DOMAIN=homeserver.local

# IP da LAN do servidor (incluído no SAN do certificado e no site do Caddy)
HS_HOST_IP=192.168.0.10

# Portas do proxy
HTTP_PORT=80
HTTPS_PORT=443
