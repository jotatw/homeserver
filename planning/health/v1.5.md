# Health Survey / Performance — v1.5

> Sprint 4 — medir e registrar histórico (não otimizar).
> Data: 2026-08-05

## Metas (referência)

| Métrica | Meta |
|---------|------|
| Homepage (TTFB) | < 1 s |
| API | < 100 ms |
| CLI | < 300 ms |
| Boot | registro |
| Shutdown | registro |
| Update | registro |

## Medições — v1.5 (baseline)

### Homepage (TTFB local)

| Amostra | Tempo |
|---------|-------|
| 1 | 0.014s |
| 2 | 0.009s |
| 3 | 0.007s |
| **Média** | **~10 ms** ✅ (< 1s) |

### API (`GET /api/v1/status`, com auth)

| Amostra | Tempo |
|---------|-------|
| 1 (cold start do core) | 0.685s |
| 2 (cache) | 0.003s |
| 3 (cache) | 0.003s |
| 4 (cache) | 0.004s |
| 5 (cache) | 0.004s |
| **Média (cache)** | **~3.5 ms** ✅ (< 100ms) |

> Obs.: a 1ª chamada após cache expirar envolve subprocesso do core
> (~685ms). Tolerável para endpoint de leitura.

### CLI

| Comando | Tempo |
|---------|-------|
| `hs version` | 43 ms ✅ (< 300ms) |
| `hs update check` | 844 ms ⚠ (faz git fetch/remote) |
| `hs status` | 202 ms ✅ (< 300ms) |

> `hs update check` depende de rede (git ls-remote) — acima da meta
> de 300ms, mas aceitável por ser sob demanda (não em todo comando).

### Boot / Shutdown

| Evento | Tempo | Observação |
|--------|-------|------------|
| Boot total (kernel+userspace) | 1min 6.6s | kernel 4.4s + userspace ~1min |
| Docker ativo após boot | ~1min | `systemctl show docker` |
| Shutdown | n/a | não medido nesta sprint |

> Shutdown: o religamento usa suspend S3 (rtcwake -m mem); tempo
> depende do RTC. Registro pendente de medição real (noite).

### Update

| Etapa | Tempo |
|-------|-------|
| `hs update check` | ~844 ms |
| `hs update apply` | não executado nesta sprint (em dia) |

## Comparação futura

Este arquivo é a baseline. Em cada versão seguinte (v1.6, v2.0),
repetir as medições e comparar.

| Métrica | v1.5 | v2.0 | Δ |
|---------|------|------|---|
| Homepage TTFB | ~10ms | | |
| API (cache) | ~3.5ms | | |
| CLI version | 43ms | | |
| Boot | 1min 6.6s | | |
