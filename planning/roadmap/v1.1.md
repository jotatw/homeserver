# HomeServer v1.1 — Roadmap

> **Objetivo**: Refinar a experiência do usuário sem alterar a arquitetura do sistema.

> **Regra**: cada sprint deve entregar algo que o usuário perceba imediatamente ao abrir a Homepage.

## Sprint 1 — Homepage Refinement

### Epic HS-101 — Storage `NaN` (Correção)

- **Problema**: card Storage mostra `NaN`.
- **Causa**: `format: size` do widget `customapi` retorna `NaN` para números (o formatter conta chaves, não formata bytes).
- **Correção**: API retorna contagens; card usa `format: number` (ou sem format).
- **Verificação**: sem `NaN`; valores consistentes.

### Epic HS-102 — Bookmarks `undefined URL` (Correção)

- **Problema**: `TypeError: URL constructor: undefined` no console.
- **Causa**: `bookmarks/item.jsx` → `description ?? new URL(bookmark.href).hostname`; bookmarks sem `description` disparam `new URL`.
- **Correção**: `description` em todos os bookmarks.
- **Verificação**: console limpo.

### Epic HS-103 — Links e ícones (Correção)

- Revisão de `href`, ícone, título, descrição e destino de todos os cards/bookmarks.
- Ícones `mdi-*`/`sh-*` são servidos via CDN (jsdelivr/selfhst) — exigem internet no navegador; robustez offline fica no backlog.

### Epic HS-104 — Card Storage (Melhoria)

```
Storage
  Pronto:       Sim
  Usuários:     2
  Compartilhado: 1
  Mídia:        0
  Documentos:   0
```

- Layout fixo (não muda mais). Fonte: `GET /api/v1/storage`.

### Epic HS-105 — Card Arquivos (Melhoria)

```
Meus Arquivos
  Utilizado: 2,3 GB
```

- Fonte: `storage.total_size_human`. Contagem de arquivos fica para o futuro.

### Epic HS-106 — Card Dispositivos (Melhoria)

```
Dispositivos
  CCCOMA... → usb
```

- Preview via `GET /api/v1/devices` (dynamic-list). Estado "Nenhum conectado" sem dispositivos.

### Epic HS-107 — Backlog + CHANGELOG (Estrutura)

- `planning/roadmap/` e `planning/backlog/` criados.
- CHANGELOG em formato Keep a Changelog.

## Sprints futuros (v1.1)

- **Sprint 2 — Navegação**: cards clicáveis, hover, feedback, estados.
- **Sprint 3 — Dashboard**: painel "Hoje", atividade, favoritos.
- **Sprint 4 — Device Preview** (estendido).
- **Sprint 5 — Minha Conta**: idioma, tema, modo padrão.
- **Sprint 6 — Performance**: cache da API, menos chamadas.
- **Sprint 7 — Polimento**: consistência, responsividade, tema.
